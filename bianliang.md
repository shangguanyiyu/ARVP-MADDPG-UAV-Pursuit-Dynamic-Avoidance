# UAV 动力学模型变量整理

## 1. UAV 位置相关变量
- $x_{i}^{k}$：UAV $i$ 在二维平面内的 x 轴位置分量（第 $k$ 时刻）。
- $y_{i}^{k}$：UAV $i$ 在二维平面内的 y 轴位置分量（第 $k$ 时刻）。
- $p_{i}^{k} = \begin{bmatrix} x_{i}^{k} \\ y_{i}^{k} \end{bmatrix}$：UAV $i$ 的二维平面位置向量（第 $k$ 时刻）。

---

## 2. UAV 速度相关变量
- $v_{i,x}^{k}$：UAV $i$ 在二维平面内的 x 轴速度分量（第 $k$ 时刻）。
- $v_{i,y}^{k}$：UAV $i$ 在二维平面内的 y 轴速度分量（第 $k$ 时刻）。
- $v_{i}^{k} = \begin{bmatrix} v_{ix}^{k} \\ v_{iy}^{k} \end{bmatrix}$：UAV $i$ 的二维平面速度向量（第 $k$ 时刻）。
- $v_{is}^{k}$：UAV $i$ 的传感器测量速度（考虑传感器延迟）。
  - $v_{isx}^{k}$：UAV $i$ 的传感器测量速度在 x 轴上的分量。

---

## 3. UAV 加速度相关变量
- $a_{i,x}^{k}$：UAV $i$ 在二维平面内的 x 轴加速度分量（第 $k$ 时刻）。
- $a_{i,y}^{k}$：UAV $i$ 在二维平面内的 y 轴加速度分量（第 $k$ 时刻）。
- $a_{i}^{k} = \begin{bmatrix} a_{ix}^{k} \\ a_{iy}^{k} \end{bmatrix}$：UAV $i$ 的二维平面加速度向量（第 $k$ 时刻）。

---

## 4. 系统状态变量
- $\mathbf{x}_{i}^{k}$：UAV $i$ 的状态向量（第 $k$ 时刻），包含位置、速度、加速度等信息。

---

## 5. 系统输入变量
- $\mathbf{u}_{i}^{k}$：UAV $i$ 的输入向量（第 $k$ 时刻）。
- $\theta _{k}^{i,\text{ref}}$：UAV $i$ 在第 $k$ 时刻的偏航角输入。
- $u_{k}^{i,\text{th}}$：UAV $i$ 在第 $k$ 时刻的油门输入信号。

---

## 6. 动力学模型相关变量
### 状态转移矩阵
- $A$：状态转移矩阵，描述系统状态随时间的演化。

### 输入矩阵
- $B$：输入矩阵，描述系统输入对状态的影响。

---

## 7. 时间相关变量
- $\Delta t$：采样时间间隔。
- $T_{d}$：传感器延迟时间。
  - GNSS 的传感器延迟时间为 $T_d = 0.3 \, \text{s}$（Li et al., 2024）。

---

## 8. 动力学模型参数
- $b_{1x}$：x 轴动力学模型中的加速度系数。
- $b_{1y}$：y 轴动力学模型中的加速度系数。

---

## 9. UAV 运动约束
- $v_{\max}$：最大速度。
- $a_{\max}$：最大加速度。
- **约束条件**：
  $$
  \left\{
  \begin{aligned}
    & \left| v^{k} \right| \le v_{\max} \\ 
    & \left| a^{k} \right| \le a_{\max} 
  \end{aligned}
  \right.
  $$



